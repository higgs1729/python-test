# データ分析試験 模擬問題集アプリ

Python3エンジニア認定 **データ分析試験** 対策用の、ローカルで動く自習アプリです。

## 使い方

### 起動方法: `start.vbs` をダブルクリック。
- **終了はアプリ画面右上の「⏻ 終了」ボタン**を押してください（サーバも停止します）。

### 簡易: ファイルを直接開く
- `index.html` をダブルクリックしても動きます。
- ただしこの場合はブラウザの localStorage に保存されます（フォルダ内 `progress.json` には保存されません）。
  ブラウザの履歴削除で消える点に注意。フォルダに残したい場合はサーバ起動を推奨します。

## 問題の追加方法
`questions.js` の `window.QUESTIONS` 配列に、次の形式で追記します。
```js
{
  id: 46,               // 一意の番号
  cat: "pandas",        // 分野キー(下表)
  q: "問題文",
  choices: ["選択肢A", "選択肢B", "選択肢C", "選択肢D"],
  answer: 0,            // 正解の選択肢インデックス(0〜3)
  exp: "解説文"
}
```
分野キー: `role / env / pybase / math / numpy / pandas / plot / ml / data`

## 注意・免責
- 本アプリの問題は **すべて学習用のオリジナル問題** です。公式の過去問や各社模擬試験の複製ではありません。
- 出題範囲は『Pythonによるあたらしいデータ分析の教科書 第2版』（翔泳社）に準拠しています。
- 本番形式の腕試しには、無料の公式・準公式模擬試験サイト（PRIME STUDY / PyQ / ExamApp / DIVE INTO EXAM）の併用を推奨します。
